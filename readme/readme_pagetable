# xv6-aarch64 Raspberry Pi 3 最终内核页表

本文说明进入 shell 前最终内核页表的层级结构、有效页表项数量、映射范围，以及页表自身占用的物理内存。

图中的箭头表示“父页表项指向下一层页表”。连续且用途相同的表项使用区间表示，并在节点中标明区间内实际包含的独立页表项数量。

## 1. 最终页表父子关系

```mermaid
flowchart TD
    TTBR["TTBR1_EL1<br/>PA 0x001ff000"]

    ROOT["L1根页表<br/>VA 0xffffff80001ff000<br/>PA 0x001ff000<br/>512项，3项有效"]

    TTBR --> ROOT

    L10["L1[0]<br/>覆盖 KERNBASE + 0～1 GiB<br/>指向L2表 PA 0x001fe000"]
    L11["L1[1]<br/>覆盖 KERNBASE + 1～2 GiB<br/>指向L2表 PA 0x001f5000"]
    L1255["L1[255]<br/>接近MAXVA的内核栈区域<br/>指向L2表 PA 0x06ffe000"]

    ROOT -->|"父项 L1[0]"| L10
    ROOT -->|"父项 L1[1]"| L11
    ROOT -->|"父项 L1[255]"| L1255

    RAM0["L2[0]<br/>2 MiB窗口<br/>内核起始区域"]
    RAMREST["L2[1]～L2[55]<br/>55个独立L2项<br/>普通RAM"]
    RAMDISK["L2[56]<br/>ramdisk 2 MiB窗口"]
    MMIO["L2[504]～L2[511]<br/>8个独立L2项<br/>BCM2837 MMIO 16 MiB"]

    L10 -->|"L2表项"| RAM0
    L10 -->|"55个L2表项"| RAMREST
    L10 -->|"L2表项"| RAMDISK
    L10 -->|"8个L2表项"| MMIO

    RAM0L3["L3表<br/>384个有效叶子<br/>L3[128]～L3[511]<br/>PA 0x00080000～0x001ff000"]
    RAMRESTL3["55张L3表<br/>每张512个有效叶子<br/>共28,160项<br/>PA 0x00200000～0x06ffffff"]
    RAMDISKL3["1张L3表<br/>256个有效叶子<br/>PA 0x07000000～0x070ff000"]
    MMIOL3["8张L3表<br/>每张512个有效叶子<br/>共4,096项<br/>PA 0x3f000000～0x3ffff000"]

    RAM0 -->|"指向1张L3表"| RAM0L3
    RAMREST -->|"每个L2项指向1张L3表"| RAMRESTL3
    RAMDISK -->|"指向1张L3表"| RAMDISKL3
    MMIO -->|"每个L2项指向1张L3表"| MMIOL3

    LOCAL["L2[0]<br/>Local peripherals窗口"]
    LOCALL3["1张L3表<br/>1个有效叶子 L3[0]<br/>PA 0x40000000"]

    L11 -->|"父项 L2[0]"| LOCAL
    LOCAL -->|"指向1张L3表"| LOCALL3

    STACKL2["L2[511]<br/>内核栈2 MiB窗口"]
    STACKL3["1张L3表<br/>64个有效叶子<br/>64个进程内核栈<br/>中间保留guard pages"]

    L1255 -->|"父项 L2[511]"| STACKL2
    STACKL2 -->|"指向1张L3表"| STACKL3
```

## 2. 文本形式的层级结构

```text
TTBR1_EL1
    │
    ▼
L1根页表：1张，512项
    │
    ├── L1[0] ──────► L2表：1张，65项有效
    │                    │
    │                    ├── L2[0] ───────► L3表：384项有效
    │                    │
    │                    ├── L2[1] ───────► L3表：512项有效
    │                    ├── L2[2] ───────► L3表：512项有效
    │                    ├── ...
    │                    ├── L2[55] ──────► L3表：512项有效
    │                    │
    │                    ├── L2[56] ──────► L3表：256项有效
    │                    │
    │                    ├── L2[504] ─────► L3表：512项有效
    │                    ├── L2[505] ─────► L3表：512项有效
    │                    ├── ...
    │                    └── L2[511] ─────► L3表：512项有效
    │
    ├── L1[1] ──────► L2表：1张，1项有效
    │                    │
    │                    └── L2[0] ───────► L3表：1项有效
    │
    └── L1[255] ────► L2表：1张，1项有效
                         │
                         └── L2[511] ─────► L3表：64项有效
```

## 3. L1为什么有3个有效项

当前页表采用39位虚拟地址：

```text
L1索引：VA[38:30]
每个L1项覆盖：1 GiB
```

3个有效L1项分别是：

| L1项 | 虚拟地址范围 | 内容 |
| ---: | --- | --- |
| `L1[0]` | `KERNBASE + 0～1 GiB` | 内核RAM、ramdisk、BCM2837 MMIO |
| `L1[1]` | `KERNBASE + 1～2 GiB` | `0x40000000` Local peripherals |
| `L1[255]` | 接近 `MAXVA` | 64个进程的内核栈和guard pages |

实际打印的起始虚拟地址为：

```text
L1[0]   VA 0xffffff8000000000
L1[1]   VA 0xffffff8040000000
L1[255] VA 0xffffffbfc0000000
```

## 4. L2为什么有67个有效Table项

每个L2项管理一个2 MiB虚拟地址窗口。

### 4.1 L1[0]下面有65项

```text
L2[0]～L2[55]       56项   内核和普通RAM
L2[56]               1项   ramdisk
L2[504]～L2[511]     8项   BCM2837 MMIO
------------------------------------------
合计                 65项
```

内核管理的RAM截止到：

```text
PHYSTOP = 0x07000000 = 112 MiB
```

因此RAM映射需要：

```text
112 MiB / 2 MiB = 56个L2项
```

ramdisk位于 `0x07000000`，进入下一个2 MiB窗口，即 `L2[56]`。

BCM2837外设窗口为16 MiB，需要8个L2项，对应 `L2[504]～L2[511]`。

### 4.2 L1[1]下面有1项

ARM Local peripherals 位于物理地址 `0x40000000`。它超过了 `L1[0]` 管理的第一个1 GiB范围，因此使用：

```text
L1[1] → L2[0] → L3表
```

### 4.3 L1[255]下面有1项

所有进程内核栈都位于接近 `MAXVA` 的同一个2 MiB窗口内，因此使用：

```text
L1[255] → L2[511] → L3表
```

最终L2有效Table项总数为：

```text
L1[0]：  65项
L1[1]：   1项
L1[255]： 1项
----------------
总计：    67项
```

每个有效L2 Table项都指向一张独立的L3页表，因此需要67张L3表。

## 5. L3有效叶子项数量

最终内核页表共有32,961个有效L3叶子项：

```text
内核和普通RAM：
  第一个2 MiB窗口       384项
  后面55个完整窗口      55 × 512 = 28,160项
  小计                  28,544项

ramdisk：
  1 MiB / 4 KiB         256项

BCM2837 MMIO：
  8 × 512               4,096项

Local peripherals：
  一页                  1项

进程内核栈：
  64个栈                 64项

总计：
  28,544 + 256 + 4,096 + 1 + 64
  = 32,961项
```

第一个2 MiB窗口只有384项有效，是因为内核从物理地址 `0x80000` 开始：

```text
2 MiB窗口共512页
0～0x7ffff占128页，不映射
0x80000～0x1fffff占384页，有效
```

因此：

```text
L3[0]～L3[127]     invalid
L3[128]            → PA 0x00080000
L3[129]            → PA 0x00081000
...
L3[511]            → PA 0x001ff000
```

## 6. 最终页表占用多少内存

每张页表包含512个64位表项：

```text
512 × 8字节 = 4096字节 = 4 KiB
```

```mermaid
flowchart LR
    TOTAL["最终内核页表<br/>71个4 KiB页面<br/>284 KiB"]

    L1MEM["L1根表<br/>1张 × 4 KiB<br/>4 KiB"]
    L2MEM["L2表<br/>3张 × 4 KiB<br/>12 KiB"]
    L3MEM["L3表<br/>67张 × 4 KiB<br/>268 KiB"]

    TOTAL --> L1MEM
    TOTAL --> L2MEM
    TOTAL --> L3MEM
```

计算如下：

```text
L1： 1张
L2： 3张
L3：67张
--------------
总计：71张

71 × 4096 = 290816字节 = 284 KiB
```

67张L3表一共有：

```text
67 × 512 = 34,304个L3槽位
```

其中：

```text
有效：32,961项
无效： 1,343项
```

## 7. 页表自身的物理位置

```mermaid
flowchart TD
    EARLY["前2 MiB早期内存<br/>PA 0x00000000～0x001fffff"]

    ROOTPAGE["L1根表<br/>PA 0x001ff000"]
    L2ZERO["L1[0]对应L2表<br/>PA 0x001fe000"]
    L2ONE["L1[1]对应L2表<br/>PA 0x001f5000"]
    EARLYL3["66张L3表<br/>PA低于0x00200000"]

    LATE["kinit2之后的普通内存"]
    L2STACK["L1[255]对应L2表<br/>PA 0x06ffe000"]
    L3STACK["内核栈L3表<br/>PA 0x06ffd000"]

    EARLY --> ROOTPAGE
    EARLY --> L2ZERO
    EARLY --> L2ONE
    EARLY --> EARLYL3

    LATE --> L2STACK
    LATE --> L3STACK
```

执行顺序为：

```c
kinit1(end, P2V(EARLYTOP));
kvminit();
kvminithart();
kinit2(P2V(EARLYTOP), P2V(PHYSTOP));
procinit();
```

`kvminit()` 在 `kinit2()` 之前执行，所以它只能使用 `kinit1()` 注册的前2 MiB内存。这时创建了：

```text
L1根表： 1张
L2表：   2张
L3表：  66张
----------------
合计：  69张 = 276 KiB
```

这些表全部位于前2 MiB。

`kinit2()` 之后，`procinit()` 为高地址进程内核栈补建：

```text
L1[255]对应的L2表：PA 0x06ffe000
L2[511]对应的L3表：PA 0x06ffd000
```

这两张表位于前2 MiB之外，共占8 KiB。

最终分布为：

```text
最终内核页表：284 KiB，共71个4 KiB页面

前2 MiB：
  69张，276 KiB

前2 MiB之外：
  2张，8 KiB
```

## 8. 父项中的地址是什么

父页表项保存的是下一层页表的物理地址，而不是内核访问该表时使用的虚拟地址。

例如：

```text
L1[0]
  保存下一层页表的 PA 0x001fe000
       │
       ▼
L2页表位于 PA 0x001fe000

L2[0]
  保存对应L3页表的物理地址
       │
       ▼
L3[128]
  保存内核第一页 PA 0x00080000
```

内核第一页的完整地址转换过程是：

```mermaid
flowchart LR
    VA["VA<br/>0xffffff8000080000"]
    L1["L1[0]"]
    L2["L2[0]"]
    L3["L3[128]"]
    PA["PA<br/>0x00080000"]

    VA --> L1
    L1 -->|"指向L2表"| L2
    L2 -->|"指向L3表"| L3
    L3 -->|"指向物理页"| PA
```

即：

```text
VA 0xffffff8000080000
        │
        ▼
L1[0]
        │
        ▼
L2[0]
        │
        ▼
L3[128]
        │
        ▼
PA 0x00080000
```

## 9. 页表本身也被页表映射

CPU硬件遍历页表时使用物理地址：

```text
TTBR1_EL1 = PA 0x001ff000
```

内核软件读取或修改页表时使用高虚拟地址：

```text
kernel_pagetable = VA 0xffffff80001ff000
```

根页表所在物理页 `0x001ff000` 也属于内核的直接映射范围：

```text
PA 0x001ff000
    ↕ 最终内核页表直接映射
VA 0xffffff80001ff000
```

因此最终页表映射了：

- 内核代码和数据。
- 普通可用RAM。
- ramdisk。
- BCM2837设备寄存器。
- 进程内核栈。
- 页表页面自身。

这不是递归页表技巧，而是因为内核将物理RAM直接映射到 `KERNBASE + PA`，页表页面恰好也位于这段物理RAM中。
