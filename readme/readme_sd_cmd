# Raspberry Pi 3 SD CMD17 超时与恢复重试

本文记录 xv6-aarch64 在 Raspberry Pi 3 真机进入 shell 后，执行 `ls` 时发生 SD 卡读取失败的诊断和解决方案。

该问题发生在以下功能都已经正常之后：

```text
EMMC Mailbox 时钟查询
SD 卡初始化与识别
MBR 分区表解析
FAT32 BPB 解析
FS.IMG cluster chain 建立
xv6 文件系统挂载
init/sh 加载
```

## 1. 最初现象

系统能够进入 shell，但执行：

```text
$ ls
```

随后 panic：

```text
panic: fat32: read FS.IMG
```

这个 panic 来自 `kernel/fat32.c`：

```c
uint32 first = b->blockno * (BSIZE / SECTOR_SIZE);

for(int i = 0; i < BSIZE / SECTOR_SIZE; i++){
  uint32 lba = file_sector_lba(first + i);
  if(sdsector(lba, b->data + i * SECTOR_SIZE, write) < 0)
    panic(write ? "fat32: write FS.IMG" :
                  "fat32: read FS.IMG");
}
```

xv6 的一个文件系统块为 1024 字节，而 SD sector 为 512 字节，因此每个 xv6 block 会转换成两个 SD sector 事务。

当时错误信息只说明 `sdsector()` 返回失败，无法判断失败发生在命令、FIFO 还是数据完成阶段。

## 2. 增加 SD 事务阶段诊断

在 `transfer_sector()` 中加入 `sd_io_phase`：

| phase | 阶段 | 主要操作 |
|---:|---|---|
| 1 | 命令阶段 | 发送 CMD17/CMD24 并等待 command complete |
| 2 | buffer-ready | 等待 read-ready/write-ready interrupt |
| 3 | PIO FIFO | 通过 EMMC_DATA 传输 512 字节 |
| 4 | transfer complete | 等待 data transfer complete |

示意实现：

```c
sd_io_phase = 1;
if(send_command(write ? CMD24 : CMD17, arg, 0) < 0)
  return -1;

sd_io_phase = 2;
if(wait_interrupt(ready, 1000000) < 0)
  return -1;

sd_io_phase = 3;
// 通过 EMMC_DATA 进行 PIO 数据传输

sd_io_phase = 4;
if(wait_interrupt(INT_DATA_DONE, 1000000) < 0)
  return -1;
```

同时在 `sdsector()` 失败时打印：

```text
LBA
读/写方向
尝试次数
失败阶段
EMMC_STATUS
EMMC_INTERRUPT
EMMC_CONTROL1
```

## 3. 得到的真实硬件错误

真机输出：

```text
sd: I/O failure lba=295956 write=0 phase=1
    status=17f0207 irq=18000 control1=e0807
panic: fat32: read FS.IMG
```

或者后续测试中的：

```text
sd: I/O retry lba=292124 write=0 attempt=1
    phase=1 status=17f0207 irq=18000 control1=e0807
```

### 3.1 phase=1

`phase=1` 表示失败发生在：

```text
CMD17：READ_SINGLE_BLOCK
```

还没有进入 PIO FIFO 数据读取阶段，因此不是 `b->data` 缓冲区或 FAT32 memcpy 的问题。

### 3.2 irq=0x18000

SDHCI interrupt/error 位：

```text
0x00008000  bit 15  ERROR interrupt summary
0x00010000  bit 16  COMMAND TIMEOUT error
```

因此：

```text
0x00018000 = ERROR + CMD_TIMEOUT
```

也就是主机发出 CMD17 后，在规定时间内没有收到有效的命令响应。

### 3.3 status=0x017f0207

错误发生后，STATUS 的低位仍包含 command/data inhibit 和 active 状态。这意味着 Arasan SDHCI 的命令或数据状态机没有自动恢复到 idle。

如果直接发送下一条命令，后续请求也会失败：

```text
一次 CMD timeout
       |
       v
CMD/DAT state machine 保持 inhibit/active
       |
       v
后续所有命令继续失败
       |
       v
内核 panic
```

## 4. 为什么不能只清中断位

清除：

```c
wr(EMMC_INTERRUPT, 0xffffffffU);
```

只能清除完成/错误状态位，不能保证 command/data state machine 已复位。

CMD timeout 后还必须使用 `CONTROL1` 的软件复位位：

```c
C1_SRST_CMD   // reset command line/state machine
C1_SRST_DATA  // reset data line/state machine
```

否则 `STATUS.CMD_INHIBIT` 或 `STATUS.DAT_INHIBIT` 可能一直保持置位。

## 5. recover_io()

在 `kernel/sd.c` 新增：

```c
static int
recover_io(void)
{
  // 清除残留的完成和错误中断。
  wr(EMMC_INTERRUPT, 0xffffffffU);

  // 同时复位 command 和 data 状态机。
  wr(EMMC_CONTROL1,
     rd(EMMC_CONTROL1) | C1_SRST_CMD | C1_SRST_DATA);

  // reset 位由硬件在完成复位后自动清零。
  if(wait_mask(EMMC_CONTROL1,
               C1_SRST_CMD | C1_SRST_DATA,
               0,
               1000000) < 0)
    return -1;

  // 清除复位过程中残留的中断状态。
  wr(EMMC_INTERRUPT, 0xffffffffU);

  // 给控制器和卡一个短暂恢复时间。
  sd_delay_us(1000);
  return 0;
}
```

它不会重新初始化整张 SD 卡，也不会重新执行：

```text
CMD0
CMD8
ACMD41
CMD2
CMD3
CMD7
```

卡仍然保持 selected/transfer 状态。`recover_io()` 只恢复 Arasan SDHCI 内部的 command/data line 状态机。

## 6. 单扇区有限重试

`sdsector()` 现在对同一扇区最多调用三次 `transfer_sector()`：

```c
int result = -1;

for(int attempt = 1; attempt <= 3; attempt++){
  result = transfer_sector(sector, (uchar *)buffer, write);
  if(result == 0)
    break;

  printf("sd: I/O retry ...");

  if(recover_io() < 0)
    break;
}
```

流程：

```text
attempt 1
  |
  +-- success --------------------------> 返回成功
  |
  +-- failure --> recover_io()
                       |
                       v
attempt 2
  |
  +-- success --------------------------> 返回成功
  |
  +-- failure --> recover_io()
                       |
                       v
attempt 3
  |
  +-- success --------------------------> 返回成功
  |
  +-- failure --> 返回失败，FAT32 层 panic
```

当前实现中第三次失败后仍可能执行一次 `recover_io()`，虽然不会再重试，但不会影响正确性；可以进一步优化为第三次失败后直接退出。

## 7. 最终真机结果

修复后执行：

```text
$ ls
```

输出：

```text
sd: I/O retry lba=292124 write=0 attempt=1
    phase=1 status=17f0207 irq=18000 control1=e0807

exec: path=ls load va=0x0 filesz=0xbd7 memsz=0xc00 off=0x78
exec: path=ls entry=0x0 sz=0x3000 sp=0x2fe0
```

随后完整列出：

```text
.
..
cat
echo
forktest
grep
init
kill
ln
ls
mkdir
rm
sh
stressfs
usertests
grind
wc
zombie
console
```

这表示：

```text
attempt 1: CMD17 timeout
              |
              v
recover_io(): CMD/DAT reset 成功
              |
              v
attempt 2: 同一 LBA 读取成功
              |
              v
exec 加载 ls 成功
              |
              v
ls 完整读取 xv6 目录并输出
```

## 8. FAT32 与 xv6 块映射关系

本系统不是把 FAT32 直接作为 xv6 文件系统，而是在 FAT32 中查找固定大小的 `FS.IMG` 文件：

```text
SD card
  |
  +-- MBR p1 FAT32
        |
        +-- FS.IMG
              |
              +-- xv6 superblock/inodes/data blocks
```

运行期读取一个 xv6 block 的路径：

```text
xv6 bcache block number
        |
        v
FS.IMG 内 sector index
        |
        v
cluster index + cluster 内偏移
        |
        v
FAT32 data-area LBA
        |
        v
CMD17 + 512-byte PIO read
```

日志中的 `lba=292124` 是整张 SD 卡的绝对 LBA，不是 FS.IMG 内的 sector 编号。

## 9. 后续改进

当前恢复机制已经能够处理偶发 CMD timeout。若重试频繁发生，还应继续检查：

1. 将正常 SD clock 从 25 MHz 临时降低到 12.5 MHz，观察 timeout 是否消失；
2. 检查 SD 卡供电稳定性；
3. 更换 SD 卡排除介质或控制器兼容性；
4. 检查 GPIO48–53 的 pinmux 与 pull 配置；
5. 在恢复后发送 CMD13 查询卡状态；
6. 区分 command timeout、command CRC、data timeout 和 data CRC；
7. 写操作重试前评估 CMD24 是否可能已经部分提交；
8. 最终支持多扇区 CMD18/CMD25 与 CMD12 stop-transmission；
9. 为连续错误增加完整卡重新初始化的最后回退路径。

## 10. 相关文件

```text
kernel/sd.c       Arasan EMMC/SDHCI、CMD17/CMD24、PIO、恢复重试
kernel/fat32.c    MBR/FAT32/FS.IMG cluster 到绝对 LBA 的转换
kernel/bio.c      xv6 buffer cache
kernel/fs.c       xv6 文件系统
kernel/log.c      xv6 文件系统日志写入
```

## 11. 总结

本次故障不是 FAT32 无法找到 `FS.IMG`，也不是 xv6 目录结构损坏。真实原因是一次偶发的 SDHCI `CMD17 command timeout` 后，控制器的 CMD/DAT 状态机没有自动恢复。

最终方案是：

```text
保存错误寄存器
      |
      v
清除 EMMC_INTERRUPT
      |
      v
复位 CMD + DAT 状态机
      |
      v
等待 reset 完成
      |
      v
重新读取同一 LBA（最多三次）
```

真机验证中，第一次读取超时，恢复后的第二次读取成功，`ls` 完整执行。
